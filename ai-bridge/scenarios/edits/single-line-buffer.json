{
  "id": "edits-single-line-buffer",
  "description": "Edge case: buffer contains a single definition with implicit; recent edits set up the pattern but there is only one more thing to edit.",
  "request": {
    "kind": "suggest",
    "buffer": "Definition only {X : Type} (n : nat) : nat := n.\n",
    "cursor": {"line": 0, "col": 0},
    "language": "rocq",
    "recent_edits": [
      {"before": "Definition foo {X : Type} (n : nat) : nat := n.",
       "after":  "Definition foo (n : nat) : nat := n."},
      {"before": "Definition bar {X : Type} (n : nat) : nat := n.",
       "after":  "Definition bar (n : nat) : nat := n."}
    ]
  },
  "expected": {
    "kind": "edits",
    "min_changes": 1,
    "max_changes": 1,
    "must_contain_each": [
      "Definition only (n : nat) : nat := n."
    ]
  }
}
