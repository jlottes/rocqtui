{
  "id": "edits-variable-rename",
  "description": "Rename a parameter (n → x) including all body uses.",
  "request": {
    "kind": "suggest",
    "buffer": "Definition double (x : nat) : nat := x + x.\nDefinition triple (x : nat) : nat := x + x + x.\nDefinition quadruple (n : nat) : nat := n + n + n + n.\nDefinition quintuple (n : nat) : nat := n + n + n + n + n.\n",
    "cursor": {"line": 0, "col": 0},
    "language": "rocq",
    "recent_edits": [
      {"before": "Definition double (n : nat) : nat := n + n.",
       "after":  "Definition double (x : nat) : nat := x + x."},
      {"before": "Definition triple (n : nat) : nat := n + n + n.",
       "after":  "Definition triple (x : nat) : nat := x + x + x."}
    ]
  },
  "expected": {
    "kind": "edits",
    "min_changes": 2,
    "max_changes": 2,
    "must_contain_each": [
      "Definition quadruple (x : nat) : nat := x + x + x + x.",
      "Definition quintuple (x : nat) : nat := x + x + x + x + x."
    ]
  }
}
