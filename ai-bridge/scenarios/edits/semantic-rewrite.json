{
  "id": "edits-semantic-rewrite",
  "description": "match-based predicate rewritten as an equation; semantic transformation (n=k).",
  "request": {
    "kind": "suggest",
    "buffer": "Definition is_zero (n : nat) : Prop := n = 0.\nDefinition is_one  (n : nat) : Prop := n = 1.\nDefinition is_two  (n : nat) : bool := match n with S (S O) => true | _ => false end.\nDefinition is_three(n : nat) : bool := match n with S (S (S O)) => true | _ => false end.\n",
    "cursor": {"line": 0, "col": 0},
    "language": "rocq",
    "recent_edits": [
      {"before": "Definition is_zero (n : nat) : bool := match n with O => true | _ => false end.",
       "after":  "Definition is_zero (n : nat) : Prop := n = 0."},
      {"before": "Definition is_one  (n : nat) : bool := match n with S O => true | _ => false end.",
       "after":  "Definition is_one  (n : nat) : Prop := n = 1."}
    ]
  },
  "expected": {
    "kind": "edits",
    "min_changes": 2,
    "max_changes": 2,
    "must_contain_each": [
      "Definition is_two  (n : nat) : Prop := n = 2.",
      "Definition is_three(n : nat) : Prop := n = 3."
    ]
  }
}
